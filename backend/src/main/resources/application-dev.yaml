# 开发环境专用配置
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/team_watermark?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# JWT 配置
jwt:
  secret: 0J6ytuN2yQa3hkTaZCIkwGpGIncu8YnB
  expiration: 86400

# 日志配置
logging:
  level:
    github.kokoachino: debug